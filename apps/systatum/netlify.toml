[build]
  base = "apps/systatum"
  command = "npm run build"
  publish = "out"
  functions = "netlify/functions"

[email]
  provider = "postmark"
  api_key = "${POSTMARK_API_KEY}"

[dev]
  functions = "netlify/functions"
